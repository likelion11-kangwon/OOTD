import React from 'react';

// 헤더에 mainView로 이동하는 back button
// 글 작성 아이콘, 검색 bar까지
const Header = () => {
    return (
        <header>
            <a href="/">back</a>
            <hr />
        </header>
    );
};

export default Header;
